get:
  tags:
    - Airports
  summary: Получение новостей по конкретному аэропорту
  description: Этот метод предоставляет возможность получить все новости об аэропорте по его IATA-коду
  operationId: getAirportNewsByIataCode
  parameters:
    - name: iataCode
      in: path
      description: IATA code
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Успешный ответ
      content:
        applicatino/json:
          schema:
            $ref: ../components/schemas/success_response.yaml
    '404':
      description: Аэропорт не найден
      content:
        applicatino/json:
          schema:
            $ref: ../components/schemas/error_response.yaml